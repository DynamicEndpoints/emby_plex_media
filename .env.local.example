# Clerk Authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx
CLERK_WEBHOOK_SECRET=whsec_xxx

# Clerk URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# Convex
# For local development:
CONVEX_DEPLOYMENT=dev:xxx
NEXT_PUBLIC_CONVEX_URL=https://xxx.convex.cloud

# For Vercel deployment - set this in Vercel dashboard:
# CONVEX_DEPLOY_KEY=prod:xxx (get from Convex dashboard -> Settings -> Deploy Key)

# Internal API key (recommended)
# Used by server-to-server calls like scheduled revocations hitting /api/revoke-access.
# If you enable this flow, set the SAME value in Convex env too:
#   npx convex env set INTERNAL_API_KEY=...
INTERNAL_API_KEY=change-me

# Stripe (optional, but required for payments/webhooks)
# Next.js uses these for the webhook endpoint: /api/webhooks/stripe
# Convex ALSO needs these for Convex HTTP actions like /stripe/sync and /stripe/webhook.
# Set them in Convex env as well:
#   npx convex env set STRIPE_SECRET_KEY=...
#   npx convex env set STRIPE_WEBHOOK_SECRET=...
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
# Get webhook secret from Stripe Dashboard -> Developers -> Webhooks
# Webhook endpoint: https://your-domain.com/api/webhooks/stripe
# Events to listen for:
#   - checkout.session.completed
#   - customer.subscription.created
#   - customer.subscription.updated
#   - customer.subscription.deleted
#   - invoice.paid
#   - invoice.payment_failed

# NOTE: Plex, Emby, and SMTP settings are stored in the Convex database
# Configure them through the Settings page in the admin dashboard
# These env vars below are OPTIONAL and only used as fallback:

# Plex Configuration (optional - configure in Settings instead)
# PLEX_URL=http://localhost:32400
# PLEX_TOKEN=your_plex_token

# Emby Configuration (optional - configure in Settings instead)
# EMBY_URL=http://localhost:8096
# EMBY_API_KEY=your_emby_api_key

# Email SMTP (optional - configure in Settings instead)
# SMTP_HOST=smtp.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# EMAIL_FROM=invites@yourdomain.com
# EMAIL_FROM_NAME=Media Invite

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# (Optional) Buy Me a Coffee webhook verification token
# BUYMEACOFFEE_WEBHOOK_TOKEN=your-token
